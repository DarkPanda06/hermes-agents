"""Maitre — a concierge with a compounding taste graph (demo cut).

Self-contained package. No dependency on core/. The demo reads only from seeded
local data in maitre/data/ — never the network. Every decision the agent makes
carries its reasoning (the "why"/"why not" strings are the product).
"""
