"""Delphi — prediction-market intern & orchestrator (demo cut).

Self-contained package. No dependency on core/. Demo reads only from cached
fixtures/snapshots in delphi/data/ — never the network.
"""
