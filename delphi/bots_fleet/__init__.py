"""Toy scraper fleet. Each bot parses a local HTML fixture into the market schema.

These simulate venue-page scrapers that break when a venue silently renames its
markup — the failure bot-doctor detects and repairs.
"""
